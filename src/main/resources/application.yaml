spring:
  application:
    name: camunda-demo
  datasource:
    url: jdbc:h2:file:./data/camunda;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080

camunda:
  bpm:
    admin-user:
      id: demo
      password: demo
      firstName: Demo
      lastName: User

logging:
  level:
    org.camunda: INFO
    com.exemplo: DEBUG
